# FTD — OpenNeuro `ds004504` (Miltiadous et al. 2023)

Already in BIDS. FTD subjects = `Group=="F"` in
[`ds004504/`](ds004504/) (23 subjects). Controls = `Group=="C"`
(29 subjects, shared with AD).

`ds004504/` is a symlink to the shared submodule at
[`../ds004504/`](../ds004504/) →
[`github.com/OpenNeuroDatasets/ds004504`](https://github.com/OpenNeuroDatasets/ds004504).
To fetch, see the parent [`README.md`](../README.md).

⚠️ **Paper typo (camera-ready fix):** the submitted text cites `ds004904`
for FTD, but that ID doesn't exist — FTD ships in the same `ds004504`
as AD.
